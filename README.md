# From Li_xin
* wmgit
```py
工作目錄下新增config.py
    user = '${user}'
    passwd = '${password}'
    domain = '${git_domain}'
    worktree = '${path}'
    gitpath = '${git_path}'
    conf_path = '${shell_config_path}'
```
